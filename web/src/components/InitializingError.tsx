import React from 'react';
import { Typography } from '@mui/material';

const InitializingError: React.FC = () => {
  return (
    <React.Fragment>
      <Typography>
        Initializing Error Occurs.
      </Typography>
    </React.Fragment>
  );
};

export default InitializingError;
